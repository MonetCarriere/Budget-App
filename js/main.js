import BudgetTracker from "/js/BudgetTracker.js";   

//here we're creating an instance of the Budget tracker class
//we pass it the query string of "app" and what it's telling the code is that we're going to use the div with the id of app and use it as the main container for our app
new BudgetTracker("#app")